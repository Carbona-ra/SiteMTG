{% extends 'base.html.twig' %}

{% block title %}CardList index{% endblock %}

{% block body %}
    <h1>CardList index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for card_list in card_lists %}
            <tr>
                <td>{{ card_list.id }}</td>
                <td>{{ card_list.name }}</td>
                <td>
                    <a href="{{ path('app_card_list_show', {'id': card_list.id}) }}">show</a>
                    <a href="{{ path('app_card_list_edit', {'id': card_list.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_card_list_new') }}">Create new</a>
{% endblock %}
